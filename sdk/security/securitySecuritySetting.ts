// Copyright (c) Tetrate, Inc 2026 All Rights Reserved.

// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

export class SecuritySecuritySetting extends pulumi.CustomResource {
    /**
     * Get an existing SecuritySecuritySetting resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SecuritySecuritySettingState, opts?: pulumi.CustomResourceOptions): SecuritySecuritySetting {
        return new SecuritySecuritySetting(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'tsb:security/securitySecuritySetting:SecuritySecuritySetting';

    /**
     * Returns true if the given object is an instance of SecuritySecuritySetting.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SecuritySecuritySetting {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SecuritySecuritySetting.__pulumiType;
    }

    /**
     * DEPRECATED: Specifies whether the proxy workloads should accept only mutual TLS authenticated traffic or allow legacy plaintext traffic as well. This field is deprecated in favor of `authenticationSettings` and will be removed in the future release
     */
    declare public readonly authentication: pulumi.Output<string>;
    /**
     * Authentication settings is used to set workload-to-workload traffic and end-user/origin authentication configuration. Authentication settings is affected by the security setting's defined propagation strategy. If `STRICTER` is used the most restrictive AuthenticationSettings traffic mode along the configuration hierarchy will prevail. AuthenticationSettings traffic mode can only be changed from `UNSET` to `OPTIONAL` to `REQUIRED`. Authentication settings http will use replace propagation strategy.
     */
    declare public readonly authenticationSettings: pulumi.Output<outputs.security.SecuritySecuritySettingAuthenticationSettings | undefined>;
    /**
     * The set of service accounts in one or more namespaces allowed or denied to access a workload (and hence its sidecar) in the mesh. Authorization is affected by the security setting's defined propagation strategy. If `STRICTER` is used the most restrictive AuthorizationSettings mode along the configuration hierarchy will prevail. AuthorizationSettings mode can only be changed from `UNSET` to `DISABLED` to `CLUSTER` to `WORKSPACE` to `NAMESPACE` to `GROUP` to `RULES` to `CUSTOM`. Restricting two `CUSTOM` AuthorizationSettings is a special case. A service account is considered stricter if it belongs to the same namespace and it has a concrete service account other than a wildcard. Hence, the only possibilities to restrict service accounts is from "ns/\*" to "ns/svc1-sa". When two lists of service accounts are compared (a parent and a child list) only the service accounts from the child list that are stricter (following the previously mentioned rules) or equal to any of the parent defined service accounts will be used. If none of the children's service accounts is stricter or equal, then the parent defined list will be used. For instance, a parent defines "ns/\*", "ns2/svc-sa", and "./\*" service accounts, whereas a child defines "ns/svc1-sa", "ns3/svc-sa", and "./*". The effective strictest list will be "ns/svc1-sa", and "./*". "ns3/svc2-sa" is discarded because it will extend the set of service accounts allowed to access the workload/s, as parent's defined service accounts does not include "ns3/*" or "ns3/svc2-sa". Restricting two `rules` AuthorizationSettings is also a special case. To consider a `rules` stricter, both allowed and denied rules list need to be considered as stricter. Allowed rules can only be refined by subsets of the initial rule. Hence, you cannot allow more workloads, only less. For instance, if an allow rule **from** workspace `w1` **to** `w2` has been specified higher up in the hierarchy, it can only be refined with the same resource or lineage resources lower down in the resource hierarchy. Which means, if `w1` has a child security group `sg1`, only `sg1` and `w1` itself are considered as a stricter **from** resource target. In the same way, if `w2` has a child security group `sg2`, only `sg2` and `w2` itself are considered as a stricter **to** resource target. On the other hand, any provided list of denied rules lower down in the hierarchy, will be considered always stricter, because all ancestor's denied rules are also taken into consideration. Basically, stricter `rules` AuthorizationSettings can only allow the same or less, and/or deny more workloads.
     */
    declare public readonly authorization: pulumi.Output<outputs.security.SecuritySecuritySettingAuthorization | undefined>;
    /**
     * Metadata values that will be add into the Istio generated configurations. When using YAML APIs like `tctl` or `gitops`, put them into the `metadata.labels` or `metadata.annotations` instead. This field is only necessary when using gRPC APIs directly.
     */
    declare public readonly configGenerationMetadata: pulumi.Output<outputs.security.SecuritySecuritySettingConfigGenerationMetadata | undefined>;
    /**
     * A description of the resource.
     */
    declare public readonly description: pulumi.Output<string>;
    /**
     * User friendly name for the resource.
     */
    declare public readonly displayName: pulumi.Output<string>;
    /**
     * Extensions specifies all the WasmExtensions assigned to this SecuritySettings with the specific configuration for each extension. This custom configuration will override the one configured globally to the extension. Each extension has a global configuration including enabling and priority that will condition the execution of the assigned extensions.
     */
    declare public readonly extensions: pulumi.Output<outputs.security.SecuritySecuritySettingExtension[] | undefined>;
    /**
     * Short name of the SecuritySetting resource.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The parent id for the SecuritySetting resource.
     */
    declare public readonly parent: pulumi.Output<string>;
    /**
     * Propagation strategy specifies how a security setting is propagated along the configuration hierarchy. The default strategy is `REPLACE`. The propagation strategy from security settings can only be changed from `REPLACE` to `STRICTER` along the settings in the configuration hierarchy. Any security setting propagation strategy changed from the default one, higher up in the configuration hierarchy, will prevail over any other defined security setting propagation strategy further down in the configuration hierarchy. For instance, if an organization's default security setting propagation strategy is changed to `STRICTER`, a restrictive propagation strategy will be used at tenant, workspace default security settings and group security settings. `STRICTER` propagation strategy will be used even though, tenant, workspace or group security settings specifies a `REPLACE` propagation strategy. Security setting properties affected by the propagation strategy are: - Authorization - AuthenticationSettings - Extension All the other properties will use the default `REPLACE` propagation strategy. How each property affected by the propagation strategy will be restricted is explained in more detail at each property.
     */
    declare public readonly propagationStrategy: pulumi.Output<string>;
    /**
     * NOTICE: this feature is in alpha stage and under active development. it would encounter breaking changes in further release and should not be adopted in production WAF settings is used to set firewall rules.
     */
    declare public readonly waf: pulumi.Output<outputs.security.SecuritySecuritySettingWaf | undefined>;

    /**
     * Create a SecuritySecuritySetting resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SecuritySecuritySettingArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SecuritySecuritySettingArgs | SecuritySecuritySettingState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SecuritySecuritySettingState | undefined;
            resourceInputs["authentication"] = state?.authentication;
            resourceInputs["authenticationSettings"] = state?.authenticationSettings;
            resourceInputs["authorization"] = state?.authorization;
            resourceInputs["configGenerationMetadata"] = state?.configGenerationMetadata;
            resourceInputs["description"] = state?.description;
            resourceInputs["displayName"] = state?.displayName;
            resourceInputs["extensions"] = state?.extensions;
            resourceInputs["name"] = state?.name;
            resourceInputs["parent"] = state?.parent;
            resourceInputs["propagationStrategy"] = state?.propagationStrategy;
            resourceInputs["waf"] = state?.waf;
        } else {
            const args = argsOrState as SecuritySecuritySettingArgs | undefined;
            if (args?.name === undefined && !opts.urn) {
                throw new Error("Missing required property 'name'");
            }
            if (args?.parent === undefined && !opts.urn) {
                throw new Error("Missing required property 'parent'");
            }
            resourceInputs["authentication"] = args?.authentication;
            resourceInputs["authenticationSettings"] = args?.authenticationSettings;
            resourceInputs["authorization"] = args?.authorization;
            resourceInputs["configGenerationMetadata"] = args?.configGenerationMetadata;
            resourceInputs["description"] = args?.description;
            resourceInputs["displayName"] = args?.displayName;
            resourceInputs["extensions"] = args?.extensions;
            resourceInputs["name"] = args?.name;
            resourceInputs["parent"] = args?.parent;
            resourceInputs["propagationStrategy"] = args?.propagationStrategy;
            resourceInputs["waf"] = args?.waf;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SecuritySecuritySetting.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SecuritySecuritySetting resources.
 */
export interface SecuritySecuritySettingState {
    /**
     * DEPRECATED: Specifies whether the proxy workloads should accept only mutual TLS authenticated traffic or allow legacy plaintext traffic as well. This field is deprecated in favor of `authenticationSettings` and will be removed in the future release
     */
    authentication?: pulumi.Input<string | undefined>;
    /**
     * Authentication settings is used to set workload-to-workload traffic and end-user/origin authentication configuration. Authentication settings is affected by the security setting's defined propagation strategy. If `STRICTER` is used the most restrictive AuthenticationSettings traffic mode along the configuration hierarchy will prevail. AuthenticationSettings traffic mode can only be changed from `UNSET` to `OPTIONAL` to `REQUIRED`. Authentication settings http will use replace propagation strategy.
     */
    authenticationSettings?: pulumi.Input<inputs.security.SecuritySecuritySettingAuthenticationSettings | undefined>;
    /**
     * The set of service accounts in one or more namespaces allowed or denied to access a workload (and hence its sidecar) in the mesh. Authorization is affected by the security setting's defined propagation strategy. If `STRICTER` is used the most restrictive AuthorizationSettings mode along the configuration hierarchy will prevail. AuthorizationSettings mode can only be changed from `UNSET` to `DISABLED` to `CLUSTER` to `WORKSPACE` to `NAMESPACE` to `GROUP` to `RULES` to `CUSTOM`. Restricting two `CUSTOM` AuthorizationSettings is a special case. A service account is considered stricter if it belongs to the same namespace and it has a concrete service account other than a wildcard. Hence, the only possibilities to restrict service accounts is from "ns/\*" to "ns/svc1-sa". When two lists of service accounts are compared (a parent and a child list) only the service accounts from the child list that are stricter (following the previously mentioned rules) or equal to any of the parent defined service accounts will be used. If none of the children's service accounts is stricter or equal, then the parent defined list will be used. For instance, a parent defines "ns/\*", "ns2/svc-sa", and "./\*" service accounts, whereas a child defines "ns/svc1-sa", "ns3/svc-sa", and "./*". The effective strictest list will be "ns/svc1-sa", and "./*". "ns3/svc2-sa" is discarded because it will extend the set of service accounts allowed to access the workload/s, as parent's defined service accounts does not include "ns3/*" or "ns3/svc2-sa". Restricting two `rules` AuthorizationSettings is also a special case. To consider a `rules` stricter, both allowed and denied rules list need to be considered as stricter. Allowed rules can only be refined by subsets of the initial rule. Hence, you cannot allow more workloads, only less. For instance, if an allow rule **from** workspace `w1` **to** `w2` has been specified higher up in the hierarchy, it can only be refined with the same resource or lineage resources lower down in the resource hierarchy. Which means, if `w1` has a child security group `sg1`, only `sg1` and `w1` itself are considered as a stricter **from** resource target. In the same way, if `w2` has a child security group `sg2`, only `sg2` and `w2` itself are considered as a stricter **to** resource target. On the other hand, any provided list of denied rules lower down in the hierarchy, will be considered always stricter, because all ancestor's denied rules are also taken into consideration. Basically, stricter `rules` AuthorizationSettings can only allow the same or less, and/or deny more workloads.
     */
    authorization?: pulumi.Input<inputs.security.SecuritySecuritySettingAuthorization | undefined>;
    /**
     * Metadata values that will be add into the Istio generated configurations. When using YAML APIs like `tctl` or `gitops`, put them into the `metadata.labels` or `metadata.annotations` instead. This field is only necessary when using gRPC APIs directly.
     */
    configGenerationMetadata?: pulumi.Input<inputs.security.SecuritySecuritySettingConfigGenerationMetadata | undefined>;
    /**
     * A description of the resource.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * User friendly name for the resource.
     */
    displayName?: pulumi.Input<string | undefined>;
    /**
     * Extensions specifies all the WasmExtensions assigned to this SecuritySettings with the specific configuration for each extension. This custom configuration will override the one configured globally to the extension. Each extension has a global configuration including enabling and priority that will condition the execution of the assigned extensions.
     */
    extensions?: pulumi.Input<pulumi.Input<inputs.security.SecuritySecuritySettingExtension>[] | undefined>;
    /**
     * Short name of the SecuritySetting resource.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The parent id for the SecuritySetting resource.
     */
    parent?: pulumi.Input<string | undefined>;
    /**
     * Propagation strategy specifies how a security setting is propagated along the configuration hierarchy. The default strategy is `REPLACE`. The propagation strategy from security settings can only be changed from `REPLACE` to `STRICTER` along the settings in the configuration hierarchy. Any security setting propagation strategy changed from the default one, higher up in the configuration hierarchy, will prevail over any other defined security setting propagation strategy further down in the configuration hierarchy. For instance, if an organization's default security setting propagation strategy is changed to `STRICTER`, a restrictive propagation strategy will be used at tenant, workspace default security settings and group security settings. `STRICTER` propagation strategy will be used even though, tenant, workspace or group security settings specifies a `REPLACE` propagation strategy. Security setting properties affected by the propagation strategy are: - Authorization - AuthenticationSettings - Extension All the other properties will use the default `REPLACE` propagation strategy. How each property affected by the propagation strategy will be restricted is explained in more detail at each property.
     */
    propagationStrategy?: pulumi.Input<string | undefined>;
    /**
     * NOTICE: this feature is in alpha stage and under active development. it would encounter breaking changes in further release and should not be adopted in production WAF settings is used to set firewall rules.
     */
    waf?: pulumi.Input<inputs.security.SecuritySecuritySettingWaf | undefined>;
}

/**
 * The set of arguments for constructing a SecuritySecuritySetting resource.
 */
export interface SecuritySecuritySettingArgs {
    /**
     * DEPRECATED: Specifies whether the proxy workloads should accept only mutual TLS authenticated traffic or allow legacy plaintext traffic as well. This field is deprecated in favor of `authenticationSettings` and will be removed in the future release
     */
    authentication?: pulumi.Input<string | undefined>;
    /**
     * Authentication settings is used to set workload-to-workload traffic and end-user/origin authentication configuration. Authentication settings is affected by the security setting's defined propagation strategy. If `STRICTER` is used the most restrictive AuthenticationSettings traffic mode along the configuration hierarchy will prevail. AuthenticationSettings traffic mode can only be changed from `UNSET` to `OPTIONAL` to `REQUIRED`. Authentication settings http will use replace propagation strategy.
     */
    authenticationSettings?: pulumi.Input<inputs.security.SecuritySecuritySettingAuthenticationSettings | undefined>;
    /**
     * The set of service accounts in one or more namespaces allowed or denied to access a workload (and hence its sidecar) in the mesh. Authorization is affected by the security setting's defined propagation strategy. If `STRICTER` is used the most restrictive AuthorizationSettings mode along the configuration hierarchy will prevail. AuthorizationSettings mode can only be changed from `UNSET` to `DISABLED` to `CLUSTER` to `WORKSPACE` to `NAMESPACE` to `GROUP` to `RULES` to `CUSTOM`. Restricting two `CUSTOM` AuthorizationSettings is a special case. A service account is considered stricter if it belongs to the same namespace and it has a concrete service account other than a wildcard. Hence, the only possibilities to restrict service accounts is from "ns/\*" to "ns/svc1-sa". When two lists of service accounts are compared (a parent and a child list) only the service accounts from the child list that are stricter (following the previously mentioned rules) or equal to any of the parent defined service accounts will be used. If none of the children's service accounts is stricter or equal, then the parent defined list will be used. For instance, a parent defines "ns/\*", "ns2/svc-sa", and "./\*" service accounts, whereas a child defines "ns/svc1-sa", "ns3/svc-sa", and "./*". The effective strictest list will be "ns/svc1-sa", and "./*". "ns3/svc2-sa" is discarded because it will extend the set of service accounts allowed to access the workload/s, as parent's defined service accounts does not include "ns3/*" or "ns3/svc2-sa". Restricting two `rules` AuthorizationSettings is also a special case. To consider a `rules` stricter, both allowed and denied rules list need to be considered as stricter. Allowed rules can only be refined by subsets of the initial rule. Hence, you cannot allow more workloads, only less. For instance, if an allow rule **from** workspace `w1` **to** `w2` has been specified higher up in the hierarchy, it can only be refined with the same resource or lineage resources lower down in the resource hierarchy. Which means, if `w1` has a child security group `sg1`, only `sg1` and `w1` itself are considered as a stricter **from** resource target. In the same way, if `w2` has a child security group `sg2`, only `sg2` and `w2` itself are considered as a stricter **to** resource target. On the other hand, any provided list of denied rules lower down in the hierarchy, will be considered always stricter, because all ancestor's denied rules are also taken into consideration. Basically, stricter `rules` AuthorizationSettings can only allow the same or less, and/or deny more workloads.
     */
    authorization?: pulumi.Input<inputs.security.SecuritySecuritySettingAuthorization | undefined>;
    /**
     * Metadata values that will be add into the Istio generated configurations. When using YAML APIs like `tctl` or `gitops`, put them into the `metadata.labels` or `metadata.annotations` instead. This field is only necessary when using gRPC APIs directly.
     */
    configGenerationMetadata?: pulumi.Input<inputs.security.SecuritySecuritySettingConfigGenerationMetadata | undefined>;
    /**
     * A description of the resource.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * User friendly name for the resource.
     */
    displayName?: pulumi.Input<string | undefined>;
    /**
     * Extensions specifies all the WasmExtensions assigned to this SecuritySettings with the specific configuration for each extension. This custom configuration will override the one configured globally to the extension. Each extension has a global configuration including enabling and priority that will condition the execution of the assigned extensions.
     */
    extensions?: pulumi.Input<pulumi.Input<inputs.security.SecuritySecuritySettingExtension>[] | undefined>;
    /**
     * Short name of the SecuritySetting resource.
     */
    name: pulumi.Input<string>;
    /**
     * The parent id for the SecuritySetting resource.
     */
    parent: pulumi.Input<string>;
    /**
     * Propagation strategy specifies how a security setting is propagated along the configuration hierarchy. The default strategy is `REPLACE`. The propagation strategy from security settings can only be changed from `REPLACE` to `STRICTER` along the settings in the configuration hierarchy. Any security setting propagation strategy changed from the default one, higher up in the configuration hierarchy, will prevail over any other defined security setting propagation strategy further down in the configuration hierarchy. For instance, if an organization's default security setting propagation strategy is changed to `STRICTER`, a restrictive propagation strategy will be used at tenant, workspace default security settings and group security settings. `STRICTER` propagation strategy will be used even though, tenant, workspace or group security settings specifies a `REPLACE` propagation strategy. Security setting properties affected by the propagation strategy are: - Authorization - AuthenticationSettings - Extension All the other properties will use the default `REPLACE` propagation strategy. How each property affected by the propagation strategy will be restricted is explained in more detail at each property.
     */
    propagationStrategy?: pulumi.Input<string | undefined>;
    /**
     * NOTICE: this feature is in alpha stage and under active development. it would encounter breaking changes in further release and should not be adopted in production WAF settings is used to set firewall rules.
     */
    waf?: pulumi.Input<inputs.security.SecuritySecuritySettingWaf | undefined>;
}
